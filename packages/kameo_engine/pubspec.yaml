name: kameo_engine
description: Moteur d'apprentissage de Kameo — Dart pur, sans Flutter ni Firebase.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
